# 🚀 NFT 系統快速開始指南

## 立即開始測試

### 1. 啟動開發服務器（如果還沒啟動）

```bash
cd /home/reyerchu/hack/hack-dev
npm run dev
# 服務器運行在 http://localhost:3009
```

### 2. 創建測試 NFT 活動

訪問管理頁面創建活動：
```
http://localhost:3009/admin/nft/campaigns
```

填寫表單：
- **活動名稱**: RWA Hackathon 紀念 NFT
- **描述**: 參與者專屬紀念 NFT
- **上傳圖片**: 選擇一張圖片
- **網路**: Sepolia（測試網）
- **符合資格的電子郵件**: 輸入測試 email（每行一個）
- **開始日期**: 現在
- **結束日期**: 一個月後
- **最大供應量**: 100

點擊「建立活動」。

### 3. 部署智能合約到測試網

```bash
cd /home/reyerchu/hack/hack-dev/contracts

# 確認環境變數已設置（在項目根目錄的 .env.local）
# DEPLOYER_PRIVATE_KEY=你的私鑰
# ETHERSCAN_API_KEY=你的 API key

# 部署到 Sepolia 測試網
npm run deploy:sepolia

# 記錄輸出的合約地址
# 輸出: ✅ 合約已部署到: 0x1234567890abcdef...
```

### 4. 更新 Firestore 中的合約地址

在 Firebase Console 中：
1. 打開 `nft-campaigns` collection
2. 找到剛創建的活動
3. 編輯文件，添加 `contractAddress` 欄位
4. 值為剛才部署的合約地址

### 5. 準備白名單地址

創建一個文本文件 `whitelist.txt`，包含測試錢包地址：
```
0x你的MetaMask錢包地址1
0x你的MetaMask錢包地址2
```

或者從 Firestore 自動同步：
```bash
cd contracts
node scripts/syncWhitelistFromFirestore.js <CAMPAIGN_ID>
```

### 6. 添加白名單到合約

```bash
cd contracts
node scripts/addWhitelist.js <合約地址> whitelist.txt
```

### 7. 啟用鑄造

```bash
cd contracts
node scripts/enableMinting.js <合約地址> true
```

### 8. 驗證設置

```bash
cd contracts
node scripts/getContractInfo.js <合約地址>
```

應該看到：
- 鑄造狀態: ✅ 已啟用
- 已鑄造數量: 0
- 最大供應量: 100

### 9. 測試用戶鑄造

1. 確保你的 MetaMask 已連接到 Sepolia 測試網
2. 確保錢包有一些測試 ETH（從 [Sepolia Faucet](https://sepoliafaucet.com/) 獲取）
3. 以白名單用戶身份登入網站
4. 訪問你的個人頁面：
   ```
   http://localhost:3009/user/<你的用戶ID>
   ```
5. 應該看到「鑄造 NFT」按鈕
6. 點擊進入鑄造頁面
7. 點擊「連接 MetaMask」
8. 在 MetaMask 中確認連接
9. 點擊「立即鑄造」
10. 在 MetaMask 中確認交易
11. 等待交易確認（約 10-30 秒）
12. 成功！🎉

### 10. 驗證鑄造結果

在 [Sepolia Etherscan](https://sepolia.etherscan.io/) 中：
- 搜索交易哈希
- 查看合約地址
- 確認 NFT 已鑄造

再次運行：
```bash
cd contracts
node scripts/getContractInfo.js <合約地址>
```

應該看到已鑄造數量增加了。

## 📝 檢查清單

部署前檢查：

### 環境準備
- [ ] Node.js 已安裝（建議 v18+）
- [ ] 已安裝所有依賴（`npm install`）
- [ ] 合約依賴已安裝（`cd contracts && npm install`）
- [ ] `.env.local` 已配置正確

### 測試網部署（Sepolia）
- [ ] 部署者錢包有 Sepolia ETH
- [ ] 已在 Admin 頁面創建活動
- [ ] 已部署智能合約
- [ ] 已在 Firestore 更新 `contractAddress`
- [ ] 已添加白名單地址
- [ ] 已啟用鑄造
- [ ] 已驗證合約狀態

### 用戶測試
- [ ] 測試錢包在白名單中
- [ ] 測試錢包有 Sepolia ETH（用於 gas）
- [ ] 可以看到「鑄造 NFT」按鈕
- [ ] 可以連接 MetaMask
- [ ] 可以成功鑄造
- [ ] 交易記錄在 Etherscan
- [ ] 鑄造記錄保存在 Firestore

### 主網部署準備（重要！）
- [ ] 已在測試網充分測試
- [ ] 已審查智能合約代碼
- [ ] 已準備足夠的 ETH 支付 gas
- [ ] 已設置多簽錢包（推薦）
- [ ] 已備份所有私鑰和配置
- [ ] 已設置監控和警報
- [ ] 團隊成員已審查部署計劃

## 🔧 常見問題

### Q: 部署失敗，顯示 "insufficient funds"
**A**: 確保部署者錢包有足夠的測試 ETH。從 [Sepolia Faucet](https://sepoliafaucet.com/) 獲取。

### Q: 用戶看不到「鑄造 NFT」按鈕
**A**: 檢查：
1. 用戶是否登入？
2. 用戶的 email 是否在活動的 `eligibleEmails` 中？
3. 活動的 `contractAddress` 是否已設置？
4. 合約的鑄造是否已啟用？

### Q: 鑄造失敗，顯示 "Not whitelisted"
**A**: 確認：
1. 用戶的錢包地址已添加到合約白名單
2. 運行 `getContractInfo.js` 檢查合約狀態

### Q: 交易一直 pending
**A**: 這可能是網路擁堵。檢查 [Etherscan](https://sepolia.etherscan.io/) 的交易狀態。可以嘗試增加 gas price。

### Q: 錢包地址如何獲取？
**A**: 用戶需要在系統中綁定錢包地址，或者使用 `syncWhitelistFromFirestore.js` 腳本從用戶數據中提取。

## 📚 相關文檔

- [完整部署指南](./contracts/DEPLOYMENT-GUIDE.md)
- [合約 README](./contracts/README.md)
- [系統完整文檔](./NFT-COMPLETE-SYSTEM.md)

## 🎉 祝你鑄造成功！

如有問題，請參考完整文檔或聯繫開發團隊。
