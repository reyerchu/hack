# ⚠️  DO NOT create contracts/.env
# All environment variables are now in the root .env.local file

# To set up your environment:
# 1. Copy .env.local.template to .env.local in the project root
# 2. Add your Etherscan API key and other credentials
# 3. Hardhat will automatically load from root .env.local

# Required variables (set these in root .env.local):
# ETHERSCAN_API_KEY=your_etherscan_api_key_here
# SEPOLIA_RPC_URL=https://ethereum-sepolia-rpc.publicnode.com
# DEPLOYER_PRIVATE_KEY=your_private_key_for_deployment (optional)
